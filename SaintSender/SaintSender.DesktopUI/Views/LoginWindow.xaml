<Window x:Class="SaintSender.DesktopUI.Views.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SaintSender.DesktopUI.Views"
        mc:Ignorable="d"
        Title="Sign In" Height="300" Width="400" FontSize="14" ResizeMode="NoResize">

    <DockPanel Margin="40 0 40 40">
        <TextBlock DockPanel.Dock="Top" Margin="0 20 0 40" Text="Log into your email account:" FontSize="20" />
        <TextBlock DockPanel.Dock="Top" Text="Email:" />
        <TextBox Name="userNameTextBox" DockPanel.Dock="Top" />
        <TextBlock DockPanel.Dock="Top" Text="Password:" Margin="0 10 0 0" />
        <PasswordBox Name="passwordBox" DockPanel.Dock="Top" VerticalAlignment="Top" />

        <StackPanel DockPanel.Dock="Bottom" Orientation="Horizontal" VerticalAlignment="Bottom" HorizontalAlignment="Center">
            <Button Margin="0 0 40 0" Padding="5 0 5 0" Click="SingIn_Click">Sign In</Button>
            <Button Padding="5 0 5 0" Click="Cancel_Click">Cancel</Button>
        </StackPanel>
    </DockPanel>
</Window>